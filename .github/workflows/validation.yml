name: Validation

on:
  push:
  pull_request:

jobs:
  validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Validation des fiches et des comptes
        run: npm run valider
      - name: Vérification des types
        run: npm run check
      - name: Tests
        run: npm test
      - name: Build
        run: npm run build
